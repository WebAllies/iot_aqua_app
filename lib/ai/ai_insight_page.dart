import 'package:flutter/material.dart';

class AiInsightPage extends StatelessWidget {
  const AiInsightPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("AI Insight"));
  }
}
